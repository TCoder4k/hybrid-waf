# Base URL of the WAF backend's Admin API — the ONLY backend the frontend talks to
# (see docs/architecture.md §15: frontend must never call ml-service, protected-api, or PostgreSQL directly)
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:3000
