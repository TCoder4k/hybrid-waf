# Used by docker-compose.yml for the postgres service.
# Copy to .env and fill in real values for local development; never commit .env.
POSTGRES_USER=waf_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hybrid_waf
